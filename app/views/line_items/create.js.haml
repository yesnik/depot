$(function () {
var $cart = $('#cart');
$('#notice').hide();

if ($cart.find('tr').size === 1) {
$cart.show('blind', 1000);
}

$cart.html("#{escape_javascript render(@cart)}");
$('#current_item').css({'background-color':'#88ff88'}).
animate({'background-color':'#114411'}, 1000);
});
