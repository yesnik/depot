%h1 Список пользователей

- if notice
  %p#notice= notice

%table
  %thead
    %tr
      %th Имя
      %th Пароль
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.password
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)
        %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to t('activerecord.attributes.user.messages.create_user'), new_user_path
